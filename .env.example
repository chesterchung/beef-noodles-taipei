# Optional: leave blank to use the built-in preview map and demo restaurant data.
# Restrict this browser key to your local origin and enable Maps JavaScript API + Places API (New).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
